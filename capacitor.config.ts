import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'iremember-FE',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
